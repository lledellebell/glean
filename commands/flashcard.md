---
allowed-tools: Read, Bash(ls:*)
description: 플래시카드 형태로 학습 항목 복습
---

# /flashcard - 플래시카드 복습

터미널에서 플래시카드 형태로 학습 항목을 복습해요.

## Arguments

- `--topic=<topic>` - 특정 토픽만 복습
- `--difficulty=<level>` - 난이도 필터 (beginner, intermediate, advanced)
- `--count=<n>` - 복습할 카드 수 (기본: 5)
- `--all` - 모든 카드 보기 (기한 무시)
- `--stats` - 통계만 표시

## Execution Flow

### 1. 통계 확인

먼저 `lib/flashcard-generator.js`의 `getFlashcardStats()`로 현재 상태를 확인해요:

```javascript
import { getFlashcardStats } from './lib/flashcard-generator.js';
const stats = getFlashcardStats();
// { total, dueToday, byTopic, byDifficulty }
```

### 2. 플래시카드 가져오기

`--stats` 옵션이면 통계만 표시하고 종료.

그 외에는 복습할 플래시카드를 가져와요:

```javascript
import { getDueFlashcards, getNextFlashcard } from './lib/flashcard-generator.js';

// 오늘 복습할 카드 목록
const cards = getDueFlashcards({ topic, difficulty });

// 또는 다음 카드 하나만
const card = getNextFlashcard({ topic, difficulty });
```

### 3. 카드 표시

각 카드를 순서대로 표시해요:

```javascript
import { formatFlashcardDisplay } from './lib/flashcard-generator.js';

// 질문만 표시
const question = formatFlashcardDisplay(card, false);

// 답변 포함 표시
const withAnswer = formatFlashcardDisplay(card, true);
```

### 4. 사용자 응답 처리

각 카드마다 사용자에게 질문하고 자기 평가를 받아요:

1. 질문 표시 (답변 숨김)
2. 힌트 표시 (있으면)
3. 사용자가 답변 준비되면 Enter
4. 답변 표시
5. 자기 평가 (1-5)

### 5. 복습 완료 처리

```javascript
import { completeReview } from './lib/learn-store.js';
completeReview(card.id, confidence);
```

## Output Format

```
╭─────────────────────────────────────╮
│ ❓ javascript                        │
├─────────────────────────────────────┤

  "Promise"이(가) 뭐예요?

  💬 힌트: 비동기

╰─────────────────────────────────────╯
  intermediate | 복습 3회

[Enter를 눌러 답변 확인]

─────────────────────────────────────

  비동기 작업의 결과를 나타내는 객체예요.
  성공(resolve) 또는 실패(reject) 상태를 가져요.

─────────────────────────────────────

이해도를 평가해주세요 (1-5):
1: 전혀 모름  2: 어려움  3: 보통  4: 쉬움  5: 완벽
```

## Statistics Display

```
📊 플래시카드 통계

전체: 42개
오늘 복습: 8개

토픽별:
javascript   ████████████████████ 15
react        ████████████ 9
typescript   ████████ 6
...

난이도별:
beginner     ████████ 8
intermediate ████████████████ 24
advanced     ████████ 10
```

## Examples

```bash
# 오늘 복습할 카드 시작
/flashcard

# javascript 토픽만
/flashcard --topic=javascript

# 초급 난이도만
/flashcard --difficulty=beginner

# 3개만 복습
/flashcard --count=3

# 통계 확인
/flashcard --stats
```
