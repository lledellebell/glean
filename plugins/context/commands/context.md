# /context - 컨텍스트 관리

세션 컨텍스트를 저장하고 복원해요.

## 사용법
```
/context <command> [options]
```

## 서브커맨드

### save - 현재 컨텍스트 저장
```bash
/context save [--name <name>] [--tag <tags>]
```

### load - 컨텍스트 복원
```bash
/context load <name|id>
```

### list - 저장된 컨텍스트 목록
```bash
/context list [--project <name>]
```

### delete - 컨텍스트 삭제
```bash
/context delete <name|id>
```

## 컨텍스트에 포함되는 정보

### 자동 수집
- 현재 작업 디렉토리
- 열린 파일 목록
- 최근 수정한 파일
- Git 브랜치/상태
- 실행 중인 작업 (Todo)

### 수동 추가 가능
- 작업 메모
- 관련 문서 링크
- 다음 할 일

## 출력 형식

### 저장 시
```
✅ 컨텍스트 저장됨

📌 이름: auth-feature
🏷️ 태그: #auth #feature
📁 프로젝트: my-app
📅 저장: 2024-01-15 15:30

포함된 정보:
- 작업 파일 5개
- Todo 항목 3개
- Git 브랜치: feature/auth

복원 명령: /context load auth-feature
```

### 목록 조회
```
## 저장된 컨텍스트

| 이름 | 프로젝트 | 날짜 | 태그 |
|------|---------|------|------|
| auth-feature | my-app | 01-15 | #auth |
| api-refactor | api-server | 01-14 | #refactor |
| bug-fix-123 | my-app | 01-13 | #bugfix |
```

### 복원 시
```
✅ 컨텍스트 복원됨: auth-feature

📁 프로젝트: /Users/me/projects/my-app
🌿 Git 브랜치: feature/auth

### 작업 중이던 파일
- src/auth/login.tsx
- src/hooks/useAuth.ts
- src/api/auth.ts

### 미완료 작업
- [ ] 로그아웃 기능 구현
- [ ] 토큰 갱신 로직 추가
- [ ] 에러 처리 개선

### 메모
JWT 리프레시 토큰은 httpOnly 쿠키에 저장하기로 결정
```

## 자동 컨텍스트

### 세션 시작 시 자동 복원
```bash
# .glean.json 설정
{
  "autoContext": {
    "enabled": true,
    "strategy": "last" | "branch" | "manual"
  }
}
```

- `last`: 마지막 컨텍스트 자동 로드
- `branch`: Git 브랜치별 컨텍스트 로드
- `manual`: 수동으로만 로드

### 세션 종료 시 자동 저장
```bash
# /done 실행 시 자동으로 컨텍스트 저장
```

## 예시

```bash
# 현재 컨텍스트 저장
/context save --name "auth-wip" --tag "auth,feature"

# 컨텍스트 복원
/context load auth-wip

# 목록 보기
/context list

# 특정 프로젝트만
/context list --project my-app

# 삭제
/context delete old-context
```

## 데이터 저장
- `~/.glean/contexts/` 에 저장
- 프로젝트별 하위 폴더
- JSON 형식

## 연동
- `/history` - 세션 히스토리에서 복원
- `/plan` - 작업 계획 포함
- `/memory` - 영구 메모리와 연동
